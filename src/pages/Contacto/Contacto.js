import Formulario from "../../components/Formu/formulario"

const Contacto = () => {
    return (
        <div>
          <Formulario />  
            
        </div>
    );
};

export default Contacto;